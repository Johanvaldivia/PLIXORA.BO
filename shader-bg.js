// Shader background disabled — replaced by static CSS dot pattern in style.css
// Keeping file to avoid 404 errors from the <script> tag in index.html
(function() {
    const canvas = document.getElementById('shader-bg');
    if (canvas) canvas.style.display = 'none';
})();

# ── Dependencias / Build ──
node_modules/
dist/
build/

# ── Sesión y secretos del bot de WhatsApp ──
whatsapp-bot/.env
whatsapp-bot/.wwebjs_auth/
whatsapp-bot/.wwebjs_cache/
whatsapp-bot/*.tar.gz

# ── Claves SSH y credenciales ──
ssh-key*
*.pem
*.key
*.crt
*.p12
secrets.env
.env.local

# ── Editor / OS ──
.obsidian/
.vscode/
.idea/
*.base
*.canvas
Thumbs.db
.DS_Store

# ── Deploy ──
deploy-netlify/